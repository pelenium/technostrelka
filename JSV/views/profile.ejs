<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Профиль</title>
    <style>
        body {
            font-family: Arial;
            overflow-x: hidden;
        }

        .menu-bar {
            overflow: hidden;
        }

        .menu-bar a {
            float: left;
            display: block;
            color: black;
            text-align: center;
            padding: 14px 16px;
            text-decoration: none;
        }

        .avatar {
            display: flex;
            flex-direction: row;
            align-items: center;
        }

        h3 {
            margin-left: 10px;
        }

        .imgAvatar {
            width: 100px;
            border-radius: 60%;
        }

        .container {
            display: flex;
            flex-direction: column;
            align-items: center;
            width: 30vw;
        }

        .gallery {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            width: 100%;
            margin: 0 auto;
            overflow-y: auto;
        }

        .galleryimg {
            width: 300px;
            height: 300px;
            margin: 10px;
            border-radius: 5px;
            cursor: pointer;
            transition: 0.3s;
        }

        .galleryimg:hover {
            opacity: 0.7;
        }

        .galleryvideo {
            width: 320px;
            height: auto;
            margin: 10px;
            cursor: pointer;
        }

        button {
            color: black;
            border: none;
            background-color: transparent;
            margin-top: 15px;
            cursor: pointer;
            margin-left: 100px;
        }

        input[type="file"] {
            display: none;
        }

        label {
            color: black;
            border: none;
            background-color: transparent;
            font-size: 20px;
            margin-top: 15px;
            cursor: pointer;
        }

        #image-viewer {
            display: none;
            position: fixed;
            z-index: 1;
            padding-top: 100px;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            overflow: auto;
            background-color: rgb(0, 0, 0);
            background-color: rgba(0, 0, 0, 0.9);
        }

        .modal-content {
            margin: auto;
            display: block;
            width: 60%;
            max-width: 600px;
            animation-name: zoom;
            animation-duration: 0.6s;
        }

        @keyframes zoom {
            from {
                transform: scale(0)
            }

            to {
                transform: scale(1)
            }
        }

        #image-viewer .close {
            position: absolute;
            top: 15px;
            right: 35px;
            color: #f1f1f1;
            font-size: 40px;
            font-weight: bold;
            transition: 0.3s;
        }

        #image-viewer .close:hover,
        #image-viewer .close:focus {
            color: #bbb;
            text-decoration: none;
            cursor: pointer;
        }

        @media only screen and (max-width: 700px) {
            .modal-content {
                width: 100%;
            }
        }

        .buttons {
            position: absolute;
            margin-top: -500px;
        }

        #share-button {
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            margin-top: 10px;
            background-color: white;
        }
    </style>
</head>

<body>

    <div class="menu-bar">
        <a href="/">Главная</a>
        <a href="/profile">Мой профиль</a>
        <a href="/editor">Редактор</a>
    </div>

    <div class="avatar">
        <h3><%= username %></h3>
    </div>
    <div class="container">
        <h1>Медиа</h1>
    </div>
    <div class="gallery">
        <% photos.forEach((photo, index) => { %>
        <div class="gallery-item">
            <img class="galleryimg" src="/<%= photo.url %>" data-index="<%= index %>">
            <form action="/profile/delete/photo" method="post">
                <input type="hidden" name="photoId" value="<%= photo.id %>">
                <button type="submit">Удалить</button>
            </form>
        </div>
        <% }); %>
        <% videos.forEach(video => { %>
        <video class="galleryvideo" controls>
            <source src="/<%= video.url %>" type="video/mp4">
            Ваш браузер такое не поддерживает
        </video>
        <% }); %>
    </div>

    <form action="/profile/upload?username=<%= username %>" method="post" enctype="multipart/form-data">
        <label for="mediaInput">Выбрать фотографию</label>
        <input type="file" id="mediaInput" name="media" accept="image/*, video/*">
        <button type="submit" value="Загрузить" style="font-size: 20px;">Загрузить</button>
    </form>

    <div id="image-viewer">
        <span class="close">&times;</span>
        <img class="modal-content" id="full-image">
        <div class="buttons">
            <button id="share-button">Поделиться</button>
            <a id="downloadLink" href="" download style="color: white; text-decoration: none; margin: 10px;">скачать</a>
        </div>
    </div>
    <script>
        let images = document.querySelectorAll('.galleryimg');

        images.forEach(function (image) {
            image.addEventListener('click', function () {
                let src = this.src;
                document.getElementById('full-image').src = src;

                let index = this.getAttribute('data-index');

                document.getElementById('downloadLink').href = src;

                document.getElementById('image-viewer').style.display = 'block';
            });
        });

        document.querySelector('#image-viewer .close').addEventListener('click', function () {
            document.getElementById('image-viewer').style.display = 'none';
        });

        document.querySelector('#share-button').addEventListener('click', function () {
            let imageSrc = document.getElementById('full-image').src;
            navigator.clipboard.writeText(imageSrc);
            alert('Готово');
        });

    </script>

</body>
</html>
