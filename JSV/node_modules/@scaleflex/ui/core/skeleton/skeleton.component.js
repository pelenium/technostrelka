import _extends from "@babel/runtime/helpers/extends";
import _objectWithoutProperties from "@babel/runtime/helpers/objectWithoutProperties";
var _excluded = ["width", "height"];
import React from 'react';
import PT from 'prop-types';
import { intrinsicComponent } from '../../utils/functions';
// import { Type } from './types';

import Styled from './skeleton.styles';
var Skeleton = intrinsicComponent(function (_ref, ref) {
  var width = _ref.width,
    height = _ref.height,
    rest = _objectWithoutProperties(_ref, _excluded);
  return /*#__PURE__*/React.createElement(Styled.Skeleton, _extends({
    width: width,
    height: height
  }, rest, {
    ref: ref
  }));
});
Skeleton.defaultProps = {};
Skeleton.propTypes = {
  width: PT.oneOfType([PT.number, PT.string]),
  height: PT.oneOfType([PT.number, PT.string])
  // type: PT.oneOf(objectValues(Type)),
};

export default Skeleton;