import { SkeletonProps } from './skeleton.props';
declare const Styled: {
    Skeleton: import("styled-components").StyledComponent<"span", import("styled-components").DefaultTheme, {
        className: string;
    } & SkeletonProps, "className">;
};
export default Styled;
