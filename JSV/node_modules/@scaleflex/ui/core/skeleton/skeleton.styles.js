import styled, { css, keyframes } from 'styled-components';
import { generateClassNames, applyDisplayNames } from '../../utils/functions';
import { Color as PC } from '../../utils/types/palette';
var baseClassName = 'Skeleton';
var pulseKeyframe = /*#__PURE__*/keyframes(["0%{opacity:1;}50%{opacity:0.4;}100%{opacity:1;}"]);
var Skeleton = /*#__PURE__*/styled.span.attrs({
  className: generateClassNames(baseClassName, 'root')
}).withConfig({
  componentId: "sc-pwn0yp-0"
})(function (_ref) {
  var theme = _ref.theme,
    width = _ref.width,
    height = _ref.height;
  return css(["display:block;background-color:", ";width:", ";height:", ";animation:", " 1.5s ease-in-out 0.5s infinite;&::before{content:'\\00a0';}"], theme.palette[PC.BordersSecondary], typeof width === 'number' ? "".concat(width, "px") : width, typeof height === 'number' ? "".concat(height, "px") : height, pulseKeyframe);
});
var Styled = applyDisplayNames({
  Skeleton: Skeleton
});
export default Styled;