import type { PopupProps } from './popup.props';
declare const Styled: {
    Popup: import("styled-components").StyledComponent<"div", import("styled-components").DefaultTheme, {
        className: string;
    } & PopupProps, "className">;
};
export default Styled;
