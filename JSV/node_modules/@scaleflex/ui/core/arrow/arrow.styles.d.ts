import type { ArrowProps } from './arrow.props';
declare const Styled: {
    Arrow: import("styled-components").StyledComponent<"span", import("styled-components").DefaultTheme, {
        className: string;
    } & ArrowProps, "className">;
};
export default Styled;
