import { InputSize } from '../../utils/types';
export var getIconSize = function getIconSize(sizeName) {
  switch (sizeName) {
    case InputSize.Md:
      return 14;
    case InputSize.Sm:
      return 12;
    default:
      return 14;
  }
};
export var getArrowSize = function getArrowSize(sizeName) {
  switch (sizeName) {
    case InputSize.Md:
      return 11;
    case InputSize.Sm:
      return 10;
    default:
      return 11;
  }
};