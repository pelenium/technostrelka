import PT from 'prop-types';
import type { SnackbarProviderProps } from './snackbar-provider.props';
declare function SnackbarProvider({ children, ...rest }: SnackbarProviderProps): JSX.Element;
declare namespace SnackbarProvider {
    var defaultProps: {
        children: undefined;
        autoHideDuration: number;
        anchorOrigin: {
            vertical: "bottom";
            horizontal: "left";
        };
    };
    var propTypes: {
        children: PT.Requireable<PT.ReactNodeLike>;
    };
}
export default SnackbarProvider;
